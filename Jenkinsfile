node
{
  stage('Build')
  {
    echo 'Build stage'
    echo "Path Env variable is set to: '$env.PATH'"
    echo "Build id for this Jenkins job is '$env.BUILD_ID'"
  }
  stage('Test')
  {
    echo 'Test stage'
  }
  stage('Deploy')
  {
    echo 'Publish stage'
  }
}
